# Broadcasting Configuration for Real-time Updates
# Add these variables to your .env file

# Set the broadcast driver to pusher or log for development
BROADCAST_DRIVER=pusher

# For development/testing, you can use a free Pusher account
# Sign up at https://pusher.com/ and get your credentials
PUSHER_APP_ID=your_pusher_app_id
PUSHER_APP_KEY=your_pusher_app_key
PUSHER_APP_SECRET=your_pusher_app_secret
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

# Frontend environment variables (add to your .env file)
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# Alternative: For testing without Pusher, you can use:
# BROADCAST_DRIVER=log
# This will log events to Laravel's log files instead of broadcasting them